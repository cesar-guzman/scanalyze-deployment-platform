# This root is non-deployable validation scaffolding for M1.
# It does not create or own backend infrastructure.
#
# State key pattern (for reference only):
#   NOT APPLICABLE — account-ready-gate does not own state.
#
# The account-ready-gate root validates preconditions only.
# It does not produce resources, contracts, or state.

# backend "s3" {
#   # NOT APPLICABLE
# }
