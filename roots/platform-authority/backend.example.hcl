# Rendered only from the reviewed platform-authority bootstrap record.
# This backend is independent from every customer deployment backend.
bucket              = "RENDERED_BY_PLATFORM_AUTHORITY_BOOTSTRAP"
key                 = "platform-authority/terraform.tfstate"
region              = "RENDERED_BY_PLATFORM_AUTHORITY_BOOTSTRAP"
encrypt             = true
kms_key_id          = "RENDERED_BY_PLATFORM_AUTHORITY_BOOTSTRAP"
use_lockfile        = true
allowed_account_ids = ["RENDERED_BY_PLATFORM_AUTHORITY_BOOTSTRAP"]
