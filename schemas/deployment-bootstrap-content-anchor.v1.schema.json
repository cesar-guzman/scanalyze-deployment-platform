{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://scanalyze.dev/schemas/deployment-bootstrap-content-anchor.v1.schema.json",
  "title": "Deployment Bootstrap Content Anchor v1",
  "description": "Minimal independently retrieved anchor for a bootstrap content request. Must match the content record_digest without embedding publication VersionIds.",
  "type": "object",
  "required": ["schema_version", "deployment_id", "record_digest"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": "1" },
    "deployment_id": { "type": "string", "pattern": "^dep_[0-9A-HJKMNP-TV-Z]{26}$" },
    "record_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" }
  }
}
