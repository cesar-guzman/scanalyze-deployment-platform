{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://scanalyze.dev/schemas/platform-authority-lambda-invocation-allowlist-release.v2.schema.json",
  "title": "Platform Authority Lambda Invocation Allowlist Release v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "record_type",
    "environment",
    "production",
    "authorization_mode",
    "two_human_status",
    "independent_approval_present",
    "single_operator_exception_digest",
    "owner_authorization_sha256",
    "active_aliases",
    "broker_artifact_manifest_digest",
    "release_status",
    "authority_account_id_digest",
    "target_region",
    "source_commit",
    "source_template_sha256",
    "source_policy_bundle_sha256",
    "broker_artifact_code_sha256",
    "broker_published_configuration_sha256",
    "collector_contract_digest",
    "collector_role_principal_digest",
    "candidate_snapshot_digest",
    "expected_authority_edges_digest",
    "allowlist_digest",
    "created_at",
    "expires_at",
    "aws_readonly_inventory_eligible",
    "live_effect_authorized",
    "deployment_authorized",
    "release_digest"
  ],
  "properties": {
    "schema_version": { "const": "2" },
    "record_type": {
      "const": "platform_authority_lambda_invocation_allowlist_release"
    },
    "environment": { "const": "non-production" },
    "production": { "const": false },
    "authorization_mode": {
      "const": "SINGLE_OPERATOR_NONPROD_EXCEPTION"
    },
    "two_human_status": { "const": "NOT_PROVEN" },
    "independent_approval_present": { "const": false },
    "single_operator_exception_digest": { "$ref": "#/$defs/digest" },
    "owner_authorization_sha256": { "$ref": "#/$defs/digest" },
    "active_aliases": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "uniqueItems": true,
      "items": {
        "enum": ["single-classify", "single-retire", "single-reconcile"]
      }
    },
    "broker_artifact_manifest_digest": { "$ref": "#/$defs/digest" },
    "release_status": { "const": "MATERIALIZED_REVIEW_REQUIRED" },
    "authority_account_id_digest": { "$ref": "#/$defs/digest" },
    "target_region": { "$ref": "#/$defs/region" },
    "source_commit": {
      "type": "string",
      "pattern": "^[a-f0-9]{40}$"
    },
    "source_template_sha256": { "$ref": "#/$defs/digest" },
    "source_policy_bundle_sha256": { "$ref": "#/$defs/digest" },
    "broker_artifact_code_sha256": { "$ref": "#/$defs/lambda_code_sha256" },
    "broker_published_configuration_sha256": { "$ref": "#/$defs/digest" },
    "collector_contract_digest": { "$ref": "#/$defs/digest" },
    "collector_role_principal_digest": { "$ref": "#/$defs/digest" },
    "candidate_snapshot_digest": { "$ref": "#/$defs/digest" },
    "expected_authority_edges_digest": { "$ref": "#/$defs/digest" },
    "allowlist_digest": { "$ref": "#/$defs/digest" },
    "created_at": { "$ref": "#/$defs/timestamp" },
    "expires_at": { "$ref": "#/$defs/timestamp" },
    "aws_readonly_inventory_eligible": { "const": true },
    "live_effect_authorized": { "const": false },
    "deployment_authorized": { "const": false },
    "release_digest": { "$ref": "#/$defs/digest" }
  },
  "$defs": {
    "digest": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "lambda_code_sha256": {
      "type": "string",
      "pattern": "^[A-Za-z0-9+/]{43}=$"
    },
    "region": {
      "type": "string",
      "pattern": "^[a-z]{2}(?:-[a-z]+)+-[0-9]+$"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "pattern": "Z$"
    }
  }
}
