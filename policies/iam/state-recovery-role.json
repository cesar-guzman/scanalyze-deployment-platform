{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ListBoundStateVersions",
      "Effect": "Allow",
      "Action": [
        "s3:ListBucket",
        "s3:ListBucketVersions"
      ],
      "Resource": "arn:${aws_partition}:s3:::scanalyze-${account_id}-tf-state",
      "Condition": {
        "StringEquals": {
          "aws:PrincipalTag/deployment_id": "${deployment_id}",
          "aws:PrincipalTag/operation": "state-recovery"
        },
        "StringLike": {
          "s3:prefix": "${deployment_id}/*/terraform.tfstate"
        }
      }
    },
    {
      "Sid": "ReadBoundStateVersions",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:GetObjectVersion"
      ],
      "Resource": "arn:${aws_partition}:s3:::scanalyze-${account_id}-tf-state/${deployment_id}/*/terraform.tfstate",
      "Condition": {
        "StringEquals": {
          "aws:PrincipalTag/deployment_id": "${deployment_id}",
          "aws:PrincipalTag/operation": "state-recovery"
        }
      }
    },
    {
      "Sid": "RestoreBoundStateVersion",
      "Effect": "Allow",
      "Action": "s3:PutObject",
      "Resource": "arn:${aws_partition}:s3:::scanalyze-${account_id}-tf-state/${deployment_id}/*/terraform.tfstate",
      "Condition": {
        "StringEquals": {
          "aws:PrincipalTag/deployment_id": "${deployment_id}",
          "aws:PrincipalTag/operation": "state-recovery",
          "s3:x-amz-server-side-encryption": "aws:kms",
          "s3:x-amz-server-side-encryption-aws-kms-key-id": "arn:${aws_partition}:kms:${region}:${account_id}:key/${state_kms_key_id}"
        }
      }
    },
    {
      "Sid": "DeleteOnlyReviewedNativeLockfile",
      "Effect": "Allow",
      "Action": "s3:DeleteObject",
      "Resource": "arn:${aws_partition}:s3:::scanalyze-${account_id}-tf-state/${deployment_id}/*/terraform.tfstate.tflock",
      "Condition": {
        "StringEquals": {
          "aws:PrincipalTag/deployment_id": "${deployment_id}",
          "aws:PrincipalTag/operation": "state-recovery",
          "aws:PrincipalTag/recovery_approved": "true"
        }
      }
    },
    {
      "Sid": "UseStateRecoveryKey",
      "Effect": "Allow",
      "Action": [
        "kms:Decrypt",
        "kms:DescribeKey",
        "kms:Encrypt",
        "kms:GenerateDataKey"
      ],
      "Resource": "arn:${aws_partition}:kms:${region}:${account_id}:key/${state_kms_key_id}",
      "Condition": {
        "StringEquals": {
          "aws:PrincipalTag/deployment_id": "${deployment_id}",
          "aws:PrincipalTag/operation": "state-recovery"
        }
      }
    },
    {
      "Sid": "DenyNonStateAccessAndRoleChaining",
      "Effect": "Deny",
      "Action": [
        "dynamodb:*",
        "ec2:*",
        "ecr:*",
        "ecs:*",
        "iam:*",
        "organizations:*",
        "sqs:*",
        "ssm:PutParameter",
        "sts:AssumeRole"
      ],
      "Resource": "*"
    }
  ]
}
